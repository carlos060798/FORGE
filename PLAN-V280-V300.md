# Plan: FORGE v2.8 + v3.0 — Fases B y C completas

> Generado el 2026-06-20. Integra Fase B del plan maestro, mejoras del informe Triple Shield y Fase C.

## Contexto

### Hallazgos: lo que ya está implementado (no repetir)
- `cli/index.js doctor` → ya implementado completo
- `post-write-conventions.js` → ya existe y ya registrado en `settings.json`
- `sdd.config.yaml` tiene 329 líneas cubriendo agentes, rutas, comportamiento, calidad, protecciones, memoria, compresión
- "API para agentes personalizados" del plan maestro → ya existe (5 pasos en `PERSONALIZACION.md`)

---

## BLOQUE 1 — Triple Shield aplicable (~1.5h)

### 1A — `chmod 777` en PROHIBIDOS (~15min)
**Archivo**: `claude-hooks/pre-tool-guard.js` array `PROHIBIDOS`
Añadir al final:
```javascript
/chmod\s+777\b/,         // permisos inseguros
/chmod\s+-R\s+777\b/,   // permisos inseguros recursivos
```
**Tests**: 2 casos en `tests/pre-tool-guard.test.js`

### 1B — Variables de entorno recomendadas (~30min)
**Archivo nuevo**: `docs/OPTIMIZACION-ENTORNO.md`
- `DISABLE_AUTOUPDATER=1` — evita actualizaciones que rompen hooks
- `CLAUDE_CODE_DISABLE_1M_CONTEXT=1` — ventana estándar (más barato)
- Aclaración: `MAX_THINKING_TOKENS` y `SessionEnd` hook NO existen en la API actual de Claude Code
**Referencia en**: `docs/README.md`

### 1C — Verificar existencia de archivo antes de Edit (~45min)
**Archivo**: `claude-hooks/pre-tool-guard.js` bloque `isWriteTool` (~línea 116)
```javascript
// Edit/MultiEdit sobre archivo inexistente → bloquear
if ((toolName === "Edit" || toolName === "MultiEdit") && filePath) {
  if (!existsSync(filePath)) {
    process.stderr.write(
      `FORGE detuvo esta acción: el archivo "${filePath}" no existe.\n` +
      `Usa Write para crear archivos nuevos, o verifica la ruta antes de editar.\n`
    );
    process.exit(2);
  }
}
```
**Tests**: Edit sobre inexistente → exit 2; Write sobre inexistente → exit 0

---

## BLOQUE 2 — Fase B: Flujos condicionales (~4h)

### 2A — Sección `sesion:` en sdd.config.yaml (~1h)
**Archivo**: `configuracion-ejemplo/sdd.config.yaml`
```yaml
sesion:
  modo: "normal"      # "normal" | "rapido" | "prototipo"
  # rapido:    salta aclarar + checklist, desactiva crítico
  # prototipo: salta crítico + seguridad + revisor, modelos a sonnet
  omitir_pasos: []    # ["checklist", "aclarar", "analizar"]
```

### 2B — Flags en comandos clave (~2h)
**`commands/sdd.planificar.md`** — CASOS ESPECIALES:
- `/sdd.planificar rapido` → omite crítico, usa sonnet para todos
- `/sdd.planificar prototipo` → omite crítico + seguridad, no crea ADR
- Lee `sesion.modo` de `sdd.config.yaml` si no hay flag explícito

**`commands/sdd.implementar.md`** — PASO 2 (modo):
- `rapido`: omite ciclo Evaluator-Optimizer, usa sonnet para grupo OPUS
- `prototipo`: omite revisor final, tester solo unitarios

**`commands/sdd.especificar.md`** — detección de modo:
- `rapido`: no genera checklist automático, no requiere `/sdd.aclarar`

### 2C — Comando `sdd.modo` (~1h)
**Archivo nuevo**: `commands/sdd.modo.md`
```
/sdd.modo rapido     → escribe sesion.modo: rapido en sdd.config.yaml
/sdd.modo normal     → restaura modo normal
/sdd.modo prototipo  → modo prototipo
/sdd.modo            → muestra modo actual
```
Registrar en `.claude-plugin/plugin.json`.
Test de consistency automático al añadirlo.

---

## BLOQUE 3 — Fase B: `sdd.configurar` mejorado (~2h)

### 3A — Subcomandos `show` y `set` (~1h)
**Archivo**: `commands/sdd.configurar.md`
```
/sdd.configurar show                              → muestra yaml completo
/sdd.configurar show agentes                      → solo sección agentes
/sdd.configurar set agentes.arquitecto.modelo opus → actualiza valor
/sdd.configurar set sesion.modo rapido             → cambia modo
```
Implementación con `sed`/`grep` en Bash (sin dependencias nuevas).

### 3B — Validación yaml en `doctor` (~1h)
**Archivo**: `cli/index.js` función `cmdDoctor()` (líneas 308-369)
Añadir al final:
- `sdd.config.yaml` existe si hay `.sdd/` inicializado
- Claves obligatorias presentes: `agentes`, `comportamiento`
- `memoria.umbral_bytes` es número positivo si está presente
- Modelos declarados son válidos: `opus`, `sonnet`, `haiku`

---

## BLOQUE 4 — Bump v2.8.0 (~20min)
- `package.json`: `2.7.0` → `2.8.0`
- `.claude-plugin/plugin.json`: versión + description
- `CHANGELOG.md`: sección v2.8.0

---

## BLOQUE 5 — Fase C v3.0 (~5.5h)

### 5A — `sdd-es init --guided` wizard interactivo (~2h)
**Archivo**: `cli/index.js` función `cmdInit()`
Flag `--guided` activa preguntas via `readline`:
- ¿Perfil? guiado / experto
- ¿Stack? Node/Python/Go/otro
- ¿Despliegue? Vercel/Railway/Docker/ninguno
- ¿Modelos? calidad alta / balanceado / económico
Genera `sdd.config.yaml` pre-configurado según respuestas.

### 5B — `sdd.estado` con dashboard ASCII (~1h)
**Archivo**: `commands/sdd.estado.md`
```
╔══════════════════════════════════════╗
║  FORGE v2.8 — Estado del Proyecto   ║
╠══════════════════════════════════════╣
║  Fase actual:    implementar         ║
║  Feature activa: auth-login          ║
║  Progreso:       T003/T007 (43%)     ║
║  Modo:           normal              ║
║  Tests:          52/52 ✅             ║
╚══════════════════════════════════════╝
```

### 5C — Community framework (~45min)
**Archivos nuevos**:
- `CONTRIBUTING.md` — cómo añadir agente/comando/reportar bug
- `.github/ISSUE_TEMPLATE/bug_report.md` — versión, comando, error, `npm test` output
- `.github/ISSUE_TEMPLATE/agent_proposal.md` — propuesta de agente nuevo

### 5D — `sdd.crear-agente` comando guiado (~1.5h)
**Archivo nuevo**: `commands/sdd.crear-agente.md`
```
/sdd.crear-agente
→ ¿Nombre? mobile-developer
→ ¿Modelo? sonnet
→ ¿Herramientas? Read, Write, Bash
→ ¿Fase de activación? implementar
→ ¿Descripción? Desarrollo de apps React Native
Crea: agents/mobile-developer.md
Actualiza: plugin.json + enrutador-agentes
```
Registrar en `plugin.json`.

### Bump v3.0.0 (~20min)
- `package.json`, `plugin.json`, `CHANGELOG.md`

---

## Orden de ejecución

```
── v2.8 ────────────────────────────────────────────────────
1A  chmod 777 en PROHIBIDOS          15min   npm test
1C  verificar existencia Edit        45min   npm test
1B  docs OPTIMIZACION-ENTORNO        30min   —
2A  sesion: en sdd.config.yaml        1h     —
2B  flags en 3 comandos .md           2h     —
2C  sdd.modo                          1h     npm test
3A  sdd.configurar show/set           1h     —
3B  doctor validación yaml            1h     npm test
4   bump 2.8.0 + CHANGELOG           20min   npm test final

── v3.0 ────────────────────────────────────────────────────
5A  init --guided wizard              2h     npm test
5B  sdd.estado dashboard ASCII        1h     —
5C  CONTRIBUTING + issue templates   45min   —
5D  sdd.crear-agente                 1.5h    npm test
    bump 3.0.0 + CHANGELOG           20min   npm test final
```

**Total v2.8**: ~7.5h | **Total v3.0**: ~5.5h | **Plan completo**: ~13h

---

## Tabla de archivos

| Archivo | Bloque | Cambio |
|---|---|---|
| `claude-hooks/pre-tool-guard.js` | 1A, 1C | patrones chmod + check existencia Edit |
| `tests/pre-tool-guard.test.js` | 1A, 1C | 4 tests nuevos |
| `docs/OPTIMIZACION-ENTORNO.md` | 1B | nuevo |
| `docs/README.md` | 1B | referencia nueva |
| `configuracion-ejemplo/sdd.config.yaml` | 2A | sección `sesion:` |
| `commands/sdd.planificar.md` | 2B | flags rapido/prototipo |
| `commands/sdd.implementar.md` | 2B | flags en PASO 2 |
| `commands/sdd.especificar.md` | 2B | detección de modo |
| `commands/sdd.modo.md` | 2C | nuevo |
| `.claude-plugin/plugin.json` | 2C, 4, 5D | sdd.modo + sdd.crear-agente + versión |
| `commands/sdd.configurar.md` | 3A | subcomandos show/set |
| `cli/index.js` | 3B, 5A | validación yaml + --guided |
| `package.json` | 4, v3 | bump versiones |
| `CHANGELOG.md` | 4, v3 | secciones v2.8 y v3.0 |
| `commands/sdd.estado.md` | 5B | dashboard ASCII |
| `CONTRIBUTING.md` | 5C | nuevo |
| `.github/ISSUE_TEMPLATE/bug_report.md` | 5C | nuevo |
| `.github/ISSUE_TEMPLATE/agent_proposal.md` | 5C | nuevo |
| `commands/sdd.crear-agente.md` | 5D | nuevo |

---

## Verificación final

```bash
npm test                           # 760+ tests pasando
node cli/index.js doctor           # valida yaml, reporta claves inválidas
node cli/index.js init --guided    # wizard interactivo funciona
```
